import React from 'react';

const StartSession = () =>(
    <div id={'divButton'}>
         <button name={'Inicia Sesión'} type={'submit'} formMethod={'POST'}> Inicia Sesión </button>
    </div>
);

export default StartSession;